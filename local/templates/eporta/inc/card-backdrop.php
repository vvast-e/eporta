<?php
if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED !== true) die();

// Единая подложка под фото товара в карточке — раньше переключалась по цвету двери (светлая
// дверь получала более тёмный фон), но в сетке карточек это выглядело пёстро/неровно — разные
// карточки с разным фоном рядом друг с другом. Один контрастный тон для всех карточек,
// достаточный, чтобы отделять и белые/светлые двери от фона, и при этом одинаковый везде.
// Светло-серый (не бежевый #ece5d6, как было раньше) — задача 06.09.2026, тот же тон, что и
// фон самой плашки карточки (.product-card в template_styles.css), чтобы карточка выглядела
// однородно-серой, как у 169.ru, а не с бежевым пятном под фото.
if (!defined("EPORTA_CARD_BACKDROP")) {
    define("EPORTA_CARD_BACKDROP", "#f0f0f0");
}
