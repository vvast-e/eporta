<?php
if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED !== true) die();

// Единая подложка под фото товара в карточке — раньше переключалась по цвету двери (светлая
// дверь получала более тёмный фон), но в сетке карточек это выглядело пёстро/неровно — разные
// карточки с разным фоном рядом друг с другом. Теперь один контрастный тон для всех карточек
// (заметно темнее прежнего базового #f6f4ef), достаточный, чтобы отделять и белые/светлые двери
// от фона, и при этом одинаковый везде.
if (!defined("EPORTA_CARD_BACKDROP")) {
    define("EPORTA_CARD_BACKDROP", "#ece5d6");
}
